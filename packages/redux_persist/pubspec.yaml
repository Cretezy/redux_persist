name: redux_persist
description: Persist Redux State with custom storage engines and serializers
version: 0.9.0
homepage: https://github.com/Cretezy/redux_persist/tree/master/packages/redux_persist

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  redux: ^5.0.0
  synchronized: ^3.0.0
  meta: ^1.3.0

dev_dependencies:
  test: ^1.16.8
