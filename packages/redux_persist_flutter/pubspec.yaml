name: redux_persist_flutter
description: Redux Persist Flutter Integration with custom storage engines
version: 0.8.3
homepage: https://github.com/Cretezy/redux_persist/tree/master/packages/redux_persist_flutter

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: "flutter"
  path_provider: "^1.6.0"
  shared_preferences: "^0.5.0"
  redux_persist: "^0.8.0"

# Used for development
#dependency_overrides:
#  redux_persist:
#    path: ../redux_persist
